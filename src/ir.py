# ir.py – internal representation utilities (placeholder)

def summarize_graph(g):
    """Return a simple textual summary of a NetworkX graph."""
    return f"Graph with {g.number_of_nodes()} nodes and {g.number_of_edges()} edges"
